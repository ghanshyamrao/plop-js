import express from 'express';
import {{camelCase name}}Controller from './controllers/{{name}}';

const router = express.Router();

// Routes for {{name}} resource
router.use('/{{name}}', {{camelCase name}}Controller);

export default router;
